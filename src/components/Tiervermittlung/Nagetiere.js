import React from 'react';
import Footer from '../Footer';

const Nagetiere = (animal) => {
    console.log(animal.animal.animal)
  return <div>
      <p>Hier sollen alle Nagetiere hin.</p>
  </div>;
};

export default Nagetiere;
