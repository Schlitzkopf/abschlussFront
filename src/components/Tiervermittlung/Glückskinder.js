import React from 'react';
import Footer from '../Footer';

const Glückskinder = (animal) => {
  return<div> 

      <p>Hier kommen die Glückskinder rein</p>
    <Footer />
    </div>;
};

export default Glückskinder;
